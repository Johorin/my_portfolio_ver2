## Where is this Application's Entrypoint?

The path of routing file in this app is bellow.
`src/Infrastructure/index.ts`

## How to DI in Node.js Express framework

`Inversify` is used for DI.

The path of DI resolver(DI container) in this app is bellow.
`src/Infrastructure/inversify.config.ts`

The detail for `Inversify`, please check bellow.
`https://github.com/inversify/InversifyJS`